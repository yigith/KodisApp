# docker-compose.yml yanına .env olarak kopyalayıp gerçek değerleri gir.
# Eksik olan varsa docker compose başlamayı reddeder.

# openssl rand -base64 48
JWT_SECRET=change-me

GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

CORS_ORIGIN=https://kod.is
